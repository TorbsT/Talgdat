using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class ComparableElement
{
    /* 
    public static bool operator <(ComparableElement<T> a, ComparableElement<T> b) => a.LessThan(b);
    public static bool operator >(ComparableElement<T> a, ComparableElement<T> b) => b.LessThan(a);

    protected abstract bool LessThan(ComparableElement<T> other);
    */
}
